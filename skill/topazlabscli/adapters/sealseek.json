{
  "schema": "portable-skill-adapter",
  "schema_version": "1.0.0",
  "platform": "sealseek",
  "mappings": [
    {
      "capability_id": "command.execute",
      "implementation": {
        "kind": "cli",
        "command": "topazlabscli",
        "fallback": "Read .topazlabscli-runtime.json from the managed Skill copy and run its bin with its node"
      },
      "features": ["local-process", "stdout-json", "exit-code", "resolution-selection", "automatic-parameter-tuning", "advanced-analysis", "bounded-profile-selection", "ab-preview", "advanced-apply"],
      "status": "implemented",
      "evidence": [
        "The Windows SealSeek runtime path is tested on the 0.2 line. Version 0.4 adds platform-neutral 2K and advanced-tuning commands, profiles, and JSON fields, but still requires a Windows SealSeek runtime rerun before those features can be labeled tested."
      ],
      "configuration": ["Install the npm package globally", "Install the bundled Skill through the CLI into the SealSeek workspace"],
      "permissions": ["local process execution", "network access to configured SSH target"],
      "normalization": "Invoke with --json and consume {ok,data,error}. On Windows with a sanitized PATH, use the CLI-managed runtime manifest in the installed Skill copy."
    },
    {
      "capability_id": "filesystem.local",
      "implementation": { "kind": "agent-native", "name": "SealSeek/OpenClaw filesystem capability" },
      "features": ["read-file", "write-file", "inspect-metadata"],
      "status": "implemented",
      "configuration": [],
      "permissions": ["input and output paths must be within the user's authorized scope"],
      "normalization": "Return absolute local paths and verified metadata."
    }
  ]
}
